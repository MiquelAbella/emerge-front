export enum MAIN_ROUTES_ENUM {
    MAP = 'map',
    PUBLISHES = 'publishes',
    BANDS = 'bands',
    CONCERTS = 'concerts',
    CONCERT_HALLS = 'concert-halls',
    USERS = 'users',
    PROFILE = 'profile'
}